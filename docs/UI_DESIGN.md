# Enterprise NotebookLM - UI/UX 设计规范

## 1. 设计原则

### 1.1 核心理念
- **清晰**: 信息层次分明，操作直观
- **高效**: 减少点击次数，支持键盘快捷键
- **专业**: 企业级视觉风格，数据密度适中
- **一致**: 统一的交互模式和视觉语言

### 1.2 设计约束
- 最小支持宽度: 1280px
- 仅支持浅色模式
- 桌面端优先

---

## 2. 色彩系统

### 2.1 主色调
```css
/* Primary - 品牌蓝 */
--primary-50:  #eff6ff;
--primary-100: #dbeafe;
--primary-200: #bfdbfe;
--primary-300: #93c5fd;
--primary-400: #60a5fa;
--primary-500: #3b82f6;  /* 主色 */
--primary-600: #2563eb;
--primary-700: #1d4ed8;
--primary-800: #1e40af;
--primary-900: #1e3a8a;

/* Neutral - 中性灰 */
--gray-50:  #f9fafb;
--gray-100: #f3f4f6;
--gray-200: #e5e7eb;
--gray-300: #d1d5db;
--gray-400: #9ca3af;
--gray-500: #6b7280;
--gray-600: #4b5563;
--gray-700: #374151;
--gray-800: #1f2937;
--gray-900: #111827;
```

### 2.2 语义色彩
```css
/* Success - 成功 */
--success-500: #22c55e;
--success-100: #dcfce7;

/* Warning - 警告 */
--warning-500: #f59e0b;
--warning-100: #fef3c7;

/* Error - 错误 */
--error-500: #ef4444;
--error-100: #fee2e2;

/* Info - 信息 */
--info-500: #3b82f6;
--info-100: #dbeafe;
```

### 2.3 数据源类型色彩
```css
/* 用于区分不同类型数据源的标识色 */
--type-csv:      #10b981;  /* 绿色 */
--type-database: #8b5cf6;  /* 紫色 */
--type-pdf:      #f59e0b;  /* 橙色 */
--type-web:      #06b6d4;  /* 青色 */
--type-generated:#ec4899;  /* 粉色 */
```

---

## 3. 字体系统

### 3.1 字体族
```css
/* 主要字体 */
--font-sans: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;

/* 代码字体 */
--font-mono: 'JetBrains Mono', 'Fira Code', Consolas, monospace;
```

### 3.2 字体大小
```css
--text-xs:   12px;  /* 辅助文字、标签 */
--text-sm:   14px;  /* 次要内容 */
--text-base: 16px;  /* 正文 */
--text-lg:   18px;  /* 小标题 */
--text-xl:   20px;  /* 标题 */
--text-2xl:  24px;  /* 大标题 */
--text-3xl:  30px;  /* 页面标题 */
```

### 3.3 行高
```css
--leading-tight:  1.25;
--leading-normal: 1.5;
--leading-relaxed: 1.75;
```

---

## 4. 间距系统

### 4.1 基础间距 (4px 基数)
```css
--space-1:  4px;
--space-2:  8px;
--space-3:  12px;
--space-4:  16px;
--space-5:  20px;
--space-6:  24px;
--space-8:  32px;
--space-10: 40px;
--space-12: 48px;
--space-16: 64px;
```

### 4.2 圆角
```css
--radius-sm:   4px;
--radius-md:   6px;
--radius-lg:   8px;
--radius-xl:   12px;
--radius-full: 9999px;
```

### 4.3 阴影
```css
--shadow-sm:  0 1px 2px rgba(0,0,0,0.05);
--shadow-md:  0 4px 6px rgba(0,0,0,0.07);
--shadow-lg:  0 10px 15px rgba(0,0,0,0.1);
--shadow-xl:  0 20px 25px rgba(0,0,0,0.15);
```

---

## 5. 整体布局

### 5.1 页面结构
```
┌─────────────────────────────────────────────────────────────────────────┐
│ Header (56px)                                                           │
├───────────────┬─────────────────────────────────┬───────────────────────┤
│               │                                 │                       │
│  Left Panel   │        Center Panel             │    Right Panel        │
│  (280-400px)  │        (flex: 1)                │    (320-480px)        │
│               │                                 │                       │
│               │                                 │                       │
│               │                                 │                       │
│               │                                 │                       │
│               │                                 │                       │
├───────────────┴─────────────────────────────────┴───────────────────────┤
│ Status Bar (32px)                                                       │
└─────────────────────────────────────────────────────────────────────────┘
```

### 5.2 面板尺寸约束
```typescript
const PANEL_CONSTRAINTS = {
  left: {
    minWidth: 280,
    defaultWidth: 320,
    maxWidth: 400,
  },
  right: {
    minWidth: 320,
    defaultWidth: 380,
    maxWidth: 480,
  },
  center: {
    minWidth: 500,
  },
};
```

---

## 6. Header 设计

### 6.1 布局
```
┌─────────────────────────────────────────────────────────────────────────┐
│ [Logo]  Enterprise NotebookLM                    [?] [Settings] [User] │
└─────────────────────────────────────────────────────────────────────────┘
     │              │                                │      │       │
     │              │                                │      │       └─ 用户头像/菜单
     │              │                                │      └───────── 设置按钮
     │              │                                └──────────────── 帮助按钮
     │              └───────────────────────────────────────────────── 应用名称
     └──────────────────────────────────────────────────────────────── Logo图标
```

### 6.2 样式规范
```css
.header {
  height: 56px;
  background: white;
  border-bottom: 1px solid var(--gray-200);
  padding: 0 var(--space-4);
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.header-logo {
  display: flex;
  align-items: center;
  gap: var(--space-3);
}

.header-title {
  font-size: var(--text-lg);
  font-weight: 600;
  color: var(--gray-900);
}

.header-actions {
  display: flex;
  align-items: center;
  gap: var(--space-2);
}
```

---

## 7. 左侧栏 - 数据源面板

### 7.1 列表视图
```
┌──────────────────────────────┐
│  DATA SOURCES                │
│  ─────────────────────────── │
│  [🔍 Search...          ]    │
│                              │
│  [+ Add Data Source]         │
│                              │
│  Sort: [Time ▼]  Filter: [All▼]│
│  ─────────────────────────── │
│                              │
│  ☐ ┌────────────────────┐   │
│    │ 📊 sales_2024.csv   │   │
│    │ 1,234 rows · 12 cols │   │
│    │ Updated 2h ago       │   │
│    └────────────────────┘   │
│                              │
│  ☑ ┌────────────────────┐   │
│    │ 🗄️ customer_db      │   │
│    │ PostgreSQL · 3 tables│   │
│    │ Connected           │   │
│    └────────────────────┘   │
│                              │
│  ☐ ┌────────────────────┐   │
│    │ 📄 Q3_report.pdf    │   │
│    │ 15 pages · 2.3 MB   │   │
│    │ Uploaded yesterday   │   │
│    └────────────────────┘   │
│                              │
│  ─────────────────────────── │
│  TAGS                        │
│  [revenue] [growth] [Q3]     │
│  [customer] [sales] [+3]     │
│                              │
└──────────────────────────────┘
```

### 7.2 数据源卡片设计
```css
.data-source-card {
  padding: var(--space-3);
  border: 1px solid var(--gray-200);
  border-radius: var(--radius-lg);
  background: white;
  cursor: pointer;
  transition: all 0.15s ease;
}

.data-source-card:hover {
  border-color: var(--primary-300);
  box-shadow: var(--shadow-sm);
}

.data-source-card.selected {
  border-color: var(--primary-500);
  background: var(--primary-50);
}

.data-source-card-header {
  display: flex;
  align-items: center;
  gap: var(--space-2);
}

.data-source-icon {
  width: 32px;
  height: 32px;
  border-radius: var(--radius-md);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 16px;
}

.data-source-icon.csv { background: #dcfce7; }
.data-source-icon.database { background: #ede9fe; }
.data-source-icon.pdf { background: #fef3c7; }
.data-source-icon.web { background: #cffafe; }

.data-source-name {
  font-weight: 500;
  color: var(--gray-900);
  font-size: var(--text-sm);
}

.data-source-meta {
  font-size: var(--text-xs);
  color: var(--gray-500);
  margin-top: var(--space-1);
}
```

### 7.3 详情视图
```
┌──────────────────────────────┐
│  [← Back]  sales_2024.csv    │
│  ─────────────────────────── │
│                              │
│  📊 CSV File                 │
│  1,234 rows · 12 columns     │
│  Last updated: 2 hours ago   │
│                              │
│  ─────────────────────────── │
│  SUMMARY                     │
│  ─────────────────────────── │
│  This dataset contains       │
│  monthly sales data for      │
│  2024, including revenue,    │
│  units sold, and regional    │
│  breakdowns across 5         │
│  product categories...       │
│  [Show more]                 │
│                              │
│  ─────────────────────────── │
│  SCHEMA                      │
│  ─────────────────────────── │
│  ┌─────────┬────────┬─────┐ │
│  │ Column  │ Type   │ Null│ │
│  ├─────────┼────────┼─────┤ │
│  │ date    │ date   │ 0%  │ │
│  │ revenue │ number │ 2%  │ │
│  │ region  │ string │ 0%  │ │
│  │ product │ string │ 0%  │ │
│  │ units   │ number │ 5%  │ │
│  └─────────┴────────┴─────┘ │
│  [View all 12 columns]       │
│                              │
│  ─────────────────────────── │
│  STATISTICS                  │
│  ─────────────────────────── │
│  revenue                     │
│  Min: $1,234  Max: $98,765   │
│  Mean: $45,678  Median: $42k │
│  ▁▂▃▅▇█▆▄▃▂ (distribution)   │
│                              │
│  ─────────────────────────── │
│  TAGS                        │
│  ─────────────────────────── │
│  [revenue] [date] [region]   │
│  [product] [units] [+ Add]   │
│                              │
└──────────────────────────────┘
```

### 7.4 添加数据源弹窗
```
┌────────────────────────────────────────────────┐
│  Add Data Source                          [×]  │
├────────────────────────────────────────────────┤
│                                                │
│  Select source type:                           │
│                                                │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│  │    📄    │  │    🗄️    │  │    📄    │    │
│  │   CSV    │  │ Database │  │   PDF    │    │
│  └──────────┘  └──────────┘  └──────────┘    │
│                                                │
│  ┌──────────┐                                  │
│  │    🌐    │                                  │
│  │   Web    │                                  │
│  └──────────┘                                  │
│                                                │
└────────────────────────────────────────────────┘
```

### 7.5 CSV 上传界面
```
┌────────────────────────────────────────────────┐
│  Upload CSV File                          [×]  │
├────────────────────────────────────────────────┤
│                                                │
│  ┌──────────────────────────────────────────┐ │
│  │                                          │ │
│  │     📄                                   │ │
│  │                                          │ │
│  │     Drag & drop your CSV file here       │ │
│  │     or click to browse                   │ │
│  │                                          │ │
│  │     Supported: .csv (max 100MB)          │ │
│  │                                          │ │
│  └──────────────────────────────────────────┘ │
│                                                │
│  ─────────────────── OR ───────────────────   │
│                                                │
│  File URL: [https://...                    ]   │
│                                                │
│                        [Cancel]  [Upload]      │
└────────────────────────────────────────────────┘
```

### 7.6 数据库连接界面
```
┌────────────────────────────────────────────────┐
│  Connect to Database                      [×]  │
├────────────────────────────────────────────────┤
│                                                │
│  Database Type:                                │
│  ○ PostgreSQL  ○ MySQL  ○ SQLite               │
│                                                │
│  Connection Details:                           │
│  ─────────────────────────────────────────     │
│  Host:     [localhost                    ]     │
│  Port:     [5432                         ]     │
│  Database: [mydb                         ]     │
│  Username: [admin                        ]     │
│  Password: [••••••••                     ]     │
│                                                │
│  ☐ Use SSL                                     │
│                                                │
│  [Test Connection]                             │
│                                                │
│  ✓ Connection successful                       │
│                                                │
│  Select tables to import:                      │
│  ☑ customers (1,234 rows)                      │
│  ☑ orders (5,678 rows)                         │
│  ☐ logs (999,999 rows)                         │
│                                                │
│                        [Cancel]  [Connect]     │
└────────────────────────────────────────────────┘
```

---

## 8. 中间栏 - 对话面板

### 8.1 整体布局
```
┌─────────────────────────────────────────────────┐
│  ┌───────────────────────────────────────────┐ │
│  │                                           │ │
│  │           Message List Area               │ │
│  │           (scrollable)                    │ │
│  │                                           │ │
│  │  ┌─────────────────────────────────────┐ │ │
│  │  │ You                                  │ │ │
│  │  │ 统计各地区的销售额并可视化展示        │ │ │
│  │  └─────────────────────────────────────┘ │ │
│  │                                           │ │
│  │  ┌─────────────────────────────────────┐ │ │
│  │  │ Assistant                           │ │ │
│  │  │                                      │ │ │
│  │  │ 根据 sales_2024.csv 数据分析，      │ │ │
│  │  │ 各地区销售额如下:                    │ │ │
│  │  │                                      │ │ │
│  │  │ [Chart: Bar Chart]                   │ │ │
│  │  │ ┌───────────────────────────────┐   │ │ │
│  │  │ │  █████████████ 华东 $2.3M     │   │ │ │
│  │  │ │  ██████████ 华南 $1.8M        │   │ │ │
│  │  │ │  ████████ 华北 $1.5M          │   │ │ │
│  │  │ │  █████ 西南 $0.9M             │   │ │ │
│  │  │ └───────────────────────────────┘   │ │ │
│  │  │                                      │ │ │
│  │  │ **关键发现:**                        │ │ │
│  │  │ - 华东地区贡献最大 (35%)             │ │ │
│  │  │ - 来源: [sales_2024.csv:L1-1234]    │ │ │
│  │  │                                      │ │ │
│  │  └─────────────────────────────────────┘ │ │
│  │                                           │ │
│  └───────────────────────────────────────────┘ │
│                                                 │
│  ┌───────────────────────────────────────────┐ │
│  │  Selected: [sales_2024.csv] [customer_db] │ │
│  ├───────────────────────────────────────────┤ │
│  │  ┌─────────────────────────────────────┐ │ │
│  │  │ Ask a question about your data...    │ │ │
│  │  │                                  [➤] │ │ │
│  │  └─────────────────────────────────────┘ │ │
│  │  [📎 Attach] [📊 Insert Table]           │ │
│  └───────────────────────────────────────────┘ │
└─────────────────────────────────────────────────┘
```

### 8.2 消息气泡设计

#### 用户消息
```css
.message-user {
  display: flex;
  justify-content: flex-end;
  margin-bottom: var(--space-4);
}

.message-user-content {
  max-width: 80%;
  padding: var(--space-3) var(--space-4);
  background: var(--primary-500);
  color: white;
  border-radius: var(--radius-lg);
  border-bottom-right-radius: var(--radius-sm);
}
```

#### Assistant 消息
```css
.message-assistant {
  display: flex;
  margin-bottom: var(--space-4);
}

.message-assistant-content {
  max-width: 90%;
  padding: var(--space-4);
  background: var(--gray-50);
  border: 1px solid var(--gray-200);
  border-radius: var(--radius-lg);
  border-bottom-left-radius: var(--radius-sm);
}

.message-assistant-header {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  margin-bottom: var(--space-2);
  font-weight: 500;
  color: var(--gray-700);
}
```

### 8.3 内嵌图表
```
┌─────────────────────────────────────────┐
│  Regional Sales Distribution      [⋮]   │
├─────────────────────────────────────────┤
│                                         │
│     ▲                                   │
│  2.5│  ████                             │
│     │  ████                             │
│  2.0│  ████ ████                        │
│     │  ████ ████                        │
│  1.5│  ████ ████ ████                   │
│     │  ████ ████ ████                   │
│  1.0│  ████ ████ ████ ████              │
│     │  ████ ████ ████ ████              │
│  0.5│  ████ ████ ████ ████              │
│     └──────────────────────────▶        │
│        华东  华南  华北  西南             │
│                                         │
│  Revenue (Millions USD)                 │
└─────────────────────────────────────────┘
```

### 8.4 内嵌表格（可编辑）
```
┌─────────────────────────────────────────────────┐
│  Monthly Summary                    [Edit] [⋮]  │
├─────────────────────────────────────────────────┤
│  ┌────────┬──────────┬────────┬───────────┐    │
│  │ Month  │ Revenue  │ Units  │ Growth    │    │
│  ├────────┼──────────┼────────┼───────────┤    │
│  │ Jan    │ $234,567 │ 1,234  │ +12%      │    │
│  │ Feb    │ $256,789 │ 1,345  │ +9%       │    │
│  │ Mar    │ $278,901 │ 1,456  │ +8%       │    │
│  │ ...    │ ...      │ ...    │ ...       │    │
│  └────────┴──────────┴────────┴───────────┘    │
│  Showing 3 of 12 rows  [Load more]              │
└─────────────────────────────────────────────────┘
```

### 8.5 数据引用链接
```css
.citation-link {
  display: inline-flex;
  align-items: center;
  gap: var(--space-1);
  padding: 2px var(--space-2);
  background: var(--primary-50);
  color: var(--primary-700);
  border-radius: var(--radius-sm);
  font-size: var(--text-xs);
  cursor: pointer;
  transition: background 0.15s;
}

.citation-link:hover {
  background: var(--primary-100);
}
```

### 8.6 输入区域
```
┌─────────────────────────────────────────────────┐
│  Active sources: [sales_2024.csv ×] [+ Add]     │
├─────────────────────────────────────────────────┤
│                                                 │
│  ┌─────────────────────────────────────────┐   │
│  │                                         │   │
│  │  Type your question here...             │   │
│  │                                         │   │
│  │                                         │   │
│  └─────────────────────────────────────────┘   │
│                                                 │
│  [📎 File] [📊 Table] [🏷️ Tag]        [Send ➤]  │
│                                                 │
└─────────────────────────────────────────────────┘
```

---

## 9. 右侧栏 - 工具面板（Studio）

### 9.1 工具面板布局

工具面板采用两列网格布局，分为三大类别：数据分析工具、内容生成工具、数据操作工具。

```
┌──────────────────────────────────────┐
│  STUDIO                        [⚙️]  │
├──────────────────────────────────────┤
│                                      │
│  ── Data Analysis ────────────────   │
│                                      │
│  ┌─────────┐  ┌─────────┐           │
│  │   📈    │  │   📉    │           │
│  │ Predict │  │   Fit   │           │
│  └─────────┘  └─────────┘           │
│                                      │
│  ┌─────────┐  ┌─────────┐           │
│  │   🎯    │  │   🔍    │           │
│  │Features │  │ Anomaly │           │
│  └─────────┘  └─────────┘           │
│                                      │
│  ┌─────────┐  ┌─────────┐           │
│  │   📊    │  │   🔗    │           │
│  │  Stats  │  │Correlate│           │
│  └─────────┘  └─────────┘           │
│                                      │
│  ── Content Generation ───────────   │
│                                      │
│  ┌─────────┐  ┌─────────┐           │
│  │   📄    │  │   📋    │           │
│  │ Report  │  │  Brief  │           │
│  └─────────┘  └─────────┘           │
│                                      │
│  ┌─────────┐  ┌─────────┐           │
│  │   🎬    │  │   🎙️    │           │
│  │   PPT   │  │  Audio  │           │
│  │         │  │  Soon   │           │
│  └─────────┘  └─────────┘           │
│                                      │
│  ── Data Operations ──────────────   │
│                                      │
│  ┌─────────┐  ┌─────────┐           │
│  │   📤    │  │   ➕    │           │
│  │ Export  │  │ Create  │           │
│  └─────────┘  └─────────┘           │
│                                      │
├──────────────────────────────────────┤
│  GENERATED CONTENT                   │
│  ──────────────────────────────────  │
│                                      │
│  ┌──────────────────────────────┐   │
│  │ 📄 Q3_Report.md              │   │
│  │    Report · 2h ago           │   │
│  └──────────────────────────────┘   │
│                                      │
│  ┌──────────────────────────────┐   │
│  │ 📊 Prediction_Results.csv    │   │
│  │    Analysis · Yesterday      │   │
│  └──────────────────────────────┘   │
│                                      │
└──────────────────────────────────────┘
```

### 9.2 工具分类与图标

| 类别 | 工具 | 图标 (Lucide) | 颜色 |
|------|------|--------------|------|
| **Data Analysis** | Prediction | TrendingUp | #3B82F6 (蓝) |
| | Curve Fitting | Spline | #8B5CF6 (紫) |
| | Feature Extraction | Sparkles | #F59E0B (橙) |
| | Anomaly Detection | AlertTriangle | #EF4444 (红) |
| | Statistical Analysis | BarChart3 | #10B981 (绿) |
| | Correlation Analysis | GitMerge | #06B6D4 (青) |
| **Content Generation** | Insights Report | FileText | #3B82F6 (蓝) |
| | One-Page Brief | FileImage | #8B5CF6 (紫) |
| | Presentation | Presentation | #F59E0B (橙) |
| | Audio Overview | Headphones | #EC4899 (粉) |
| **Data Operations** | Export Data | Download | #6B7280 (灰) |
| | Create Data Source | PlusCircle | #10B981 (绿) |

### 9.3 工具网格样式

```css
/* 工具网格容器 */
.tool-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 12px;
  padding: 0 16px;
}

/* 分类标题 */
.tool-category-title {
  font-size: 11px;
  font-weight: 600;
  color: var(--gray-400);
  text-transform: uppercase;
  letter-spacing: 0.5px;
  margin: 16px 16px 8px;
  display: flex;
  align-items: center;
  gap: 8px;
}

.tool-category-title::before,
.tool-category-title::after {
  content: '';
  flex: 1;
  height: 1px;
  background: var(--gray-200);
}

/* 工具按钮 - 方形卡片 */
.tool-button-grid {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 16px 8px;
  background: white;
  border: 1px solid var(--gray-200);
  border-radius: 12px;
  cursor: pointer;
  transition: all 0.15s ease;
  min-height: 80px;
}

.tool-button-grid:hover {
  border-color: var(--primary-300);
  background: var(--primary-50);
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(59, 130, 246, 0.15);
}

.tool-button-grid:active {
  transform: translateY(0);
}

.tool-button-grid.disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.tool-button-grid.disabled:hover {
  transform: none;
  box-shadow: none;
  border-color: var(--gray-200);
  background: white;
}

/* 图标样式 */
.tool-icon-grid {
  width: 36px;
  height: 36px;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 8px;
}

/* 不同类别的图标颜色 */
.tool-icon-grid.analysis { color: #3B82F6; }
.tool-icon-grid.generation { color: #8B5CF6; }
.tool-icon-grid.operation { color: #10B981; }

/* 标签样式 */
.tool-label-grid {
  font-size: 12px;
  font-weight: 500;
  color: var(--gray-700);
  text-align: center;
}

/* Coming Soon 徽章 */
.tool-badge {
  font-size: 9px;
  padding: 2px 6px;
  background: var(--warning-100);
  color: var(--warning-500);
  border-radius: 4px;
  margin-top: 4px;
}
```

### 9.4 单个工具按钮设计

```
┌─────────────────┐
│                 │
│       📈        │   ← 图标 24px
│                 │
│    Predict      │   ← 标签 12px
│                 │
└─────────────────┘
     约 80px 宽
```

### 9.5 旧版工具按钮样式（列表模式，保留兼容）

```css
.tool-button {
  display: flex;
  align-items: center;
  gap: var(--space-3);
  width: 100%;
  padding: var(--space-3) var(--space-4);
  background: white;
  border: 1px solid var(--gray-200);
  border-radius: var(--radius-lg);
  cursor: pointer;
  transition: all 0.15s;
  text-align: left;
}

.tool-button:hover {
  border-color: var(--primary-300);
  background: var(--primary-50);
}

.tool-button:active {
  background: var(--primary-100);
}

.tool-button.disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.tool-button-icon {
  font-size: 20px;
}

.tool-button-label {
  font-weight: 500;
  color: var(--gray-800);
}

.tool-button-sublabel {
  font-size: var(--text-xs);
  color: var(--gray-500);
}
```

### 9.6 详情模式（点击引用时）
```
┌──────────────────────────────┐
│  [← Tools]  Data Detail      │
│  ─────────────────────────── │
│                              │
│  Source: sales_2024.csv      │
│  Rows: 45-67                 │
│  Columns: revenue, region    │
│                              │
│  ─────────────────────────── │
│  DATA PREVIEW                │
│  ─────────────────────────── │
│                              │
│  ┌──────┬─────────┬───────┐ │
│  │ Row  │ Revenue │Region │ │
│  ├──────┼─────────┼───────┤ │
│  │ 45   │ $12,345 │ 华东  │ │  ← highlighted
│  │ 46   │ $23,456 │ 华东  │ │  ← highlighted
│  │ 47   │ $34,567 │ 华南  │ │
│  │ ...  │ ...     │ ...   │ │
│  └──────┴─────────┴───────┘ │
│                              │
│  ─────────────────────────── │
│  COLUMN STATS                │
│  ─────────────────────────── │
│                              │
│  revenue (selected range)    │
│  Sum: $234,567               │
│  Avg: $10,198                │
│  Min: $5,432  Max: $45,678   │
│                              │
│  ─────────────────────────── │
│  ACTIONS                     │
│  ─────────────────────────── │
│                              │
│  [Ask about this data]       │
│  [Export selection]          │
│  [View full dataset]         │
│                              │
└──────────────────────────────┘
```

### 9.7 生成内容项设计
```css
.generated-item {
  display: flex;
  align-items: center;
  gap: var(--space-3);
  padding: var(--space-2) var(--space-3);
  border-radius: var(--radius-md);
  cursor: pointer;
  transition: background 0.15s;
}

.generated-item:hover {
  background: var(--gray-100);
}

.generated-item-icon {
  width: 32px;
  height: 32px;
  border-radius: var(--radius-md);
  background: var(--gray-100);
  display: flex;
  align-items: center;
  justify-content: center;
}

.generated-item-info {
  flex: 1;
  min-width: 0;
}

.generated-item-name {
  font-weight: 500;
  color: var(--gray-800);
  font-size: var(--text-sm);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.generated-item-meta {
  font-size: var(--text-xs);
  color: var(--gray-500);
}

.generated-item-actions {
  opacity: 0;
  transition: opacity 0.15s;
}

.generated-item:hover .generated-item-actions {
  opacity: 1;
}
```

---

## 10. 状态栏设计

```
┌─────────────────────────────────────────────────────────────────────────┐
│  ● Connected to 3 data sources     │ Last sync: 2 min ago    │ v0.1.0  │
└─────────────────────────────────────────────────────────────────────────┘
```

```css
.status-bar {
  height: 32px;
  background: var(--gray-50);
  border-top: 1px solid var(--gray-200);
  padding: 0 var(--space-4);
  display: flex;
  align-items: center;
  justify-content: space-between;
  font-size: var(--text-xs);
  color: var(--gray-600);
}

.status-indicator {
  display: flex;
  align-items: center;
  gap: var(--space-2);
}

.status-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
}

.status-dot.connected { background: var(--success-500); }
.status-dot.disconnected { background: var(--error-500); }
.status-dot.loading { background: var(--warning-500); animation: pulse 1s infinite; }
```

---

## 11. 通用组件规范

### 11.1 按钮
```
Primary:    [█████████████]  蓝底白字
Secondary:  [░░░░░░░░░░░░░]  白底蓝边蓝字
Ghost:      [             ]  透明底灰字
Danger:     [█████████████]  红底白字
```

```css
.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-2);
  padding: var(--space-2) var(--space-4);
  font-size: var(--text-sm);
  font-weight: 500;
  border-radius: var(--radius-md);
  cursor: pointer;
  transition: all 0.15s;
}

.btn-primary {
  background: var(--primary-500);
  color: white;
  border: none;
}

.btn-primary:hover {
  background: var(--primary-600);
}

.btn-secondary {
  background: white;
  color: var(--primary-600);
  border: 1px solid var(--primary-300);
}

.btn-secondary:hover {
  background: var(--primary-50);
}

.btn-ghost {
  background: transparent;
  color: var(--gray-600);
  border: none;
}

.btn-ghost:hover {
  background: var(--gray-100);
}
```

### 11.2 输入框
```css
.input {
  width: 100%;
  padding: var(--space-2) var(--space-3);
  font-size: var(--text-sm);
  border: 1px solid var(--gray-300);
  border-radius: var(--radius-md);
  background: white;
  transition: all 0.15s;
}

.input:focus {
  outline: none;
  border-color: var(--primary-500);
  box-shadow: 0 0 0 3px var(--primary-100);
}

.input::placeholder {
  color: var(--gray-400);
}
```

### 11.3 复选框
```css
.checkbox {
  display: inline-flex;
  align-items: center;
  gap: var(--space-2);
  cursor: pointer;
}

.checkbox-input {
  width: 16px;
  height: 16px;
  border: 2px solid var(--gray-300);
  border-radius: var(--radius-sm);
  transition: all 0.15s;
}

.checkbox-input:checked {
  background: var(--primary-500);
  border-color: var(--primary-500);
}

.checkbox-label {
  font-size: var(--text-sm);
  color: var(--gray-700);
}
```

### 11.4 标签/徽章
```css
.tag {
  display: inline-flex;
  align-items: center;
  gap: var(--space-1);
  padding: 2px var(--space-2);
  font-size: var(--text-xs);
  font-weight: 500;
  border-radius: var(--radius-full);
  background: var(--gray-100);
  color: var(--gray-700);
}

.tag.clickable {
  cursor: pointer;
  transition: all 0.15s;
}

.tag.clickable:hover {
  background: var(--primary-100);
  color: var(--primary-700);
}

.tag.active {
  background: var(--primary-500);
  color: white;
}
```

### 11.5 模态框
```css
.modal-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
}

.modal {
  background: white;
  border-radius: var(--radius-xl);
  box-shadow: var(--shadow-xl);
  max-width: 90vw;
  max-height: 90vh;
  overflow: hidden;
  display: flex;
  flex-direction: column;
}

.modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: var(--space-4);
  border-bottom: 1px solid var(--gray-200);
}

.modal-title {
  font-size: var(--text-lg);
  font-weight: 600;
  color: var(--gray-900);
}

.modal-body {
  padding: var(--space-4);
  overflow-y: auto;
  flex: 1;
}

.modal-footer {
  display: flex;
  justify-content: flex-end;
  gap: var(--space-2);
  padding: var(--space-4);
  border-top: 1px solid var(--gray-200);
}
```

---

## 12. 加载状态

### 12.1 Skeleton 加载
```css
.skeleton {
  background: linear-gradient(
    90deg,
    var(--gray-200) 25%,
    var(--gray-100) 50%,
    var(--gray-200) 75%
  );
  background-size: 200% 100%;
  animation: skeleton-loading 1.5s infinite;
  border-radius: var(--radius-md);
}

@keyframes skeleton-loading {
  0% { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}
```

### 12.2 消息流式加载
```
┌─────────────────────────────────────┐
│ Assistant                           │
│                                     │
│ 根据数据分析，我发现了以下趋势█      │
│                                     │
│ [Generating...]                     │
└─────────────────────────────────────┘
```

---

## 13. 响应式行为

### 13.1 面板折叠
- 双击面板边缘可折叠/展开
- 折叠后显示图标条
- 键盘快捷键支持

### 13.2 宽度不足提示
```
┌─────────────────────────────────────┐
│                                     │
│     ⚠️                              │
│                                     │
│     窗口宽度不足                     │
│                                     │
│     请将窗口宽度调整至               │
│     1280px 以上以获得               │
│     最佳体验                        │
│                                     │
│     当前: 1024px                    │
│     最小: 1280px                    │
│                                     │
└─────────────────────────────────────┘
```

---

## 14. 键盘快捷键

| 快捷键 | 功能 |
|-------|------|
| `Cmd/Ctrl + K` | 打开搜索 |
| `Cmd/Ctrl + N` | 新建数据源 |
| `Cmd/Ctrl + Enter` | 发送消息 |
| `Cmd/Ctrl + 1` | 聚焦左侧栏 |
| `Cmd/Ctrl + 2` | 聚焦中间栏 |
| `Cmd/Ctrl + 3` | 聚焦右侧栏 |
| `Esc` | 关闭模态框/取消操作 |
| `Cmd/Ctrl + Z` | 撤销 |

---

## 15. 动画规范

```css
/* 过渡时长 */
--duration-fast: 150ms;
--duration-normal: 250ms;
--duration-slow: 350ms;

/* 缓动函数 */
--ease-default: cubic-bezier(0.4, 0, 0.2, 1);
--ease-in: cubic-bezier(0.4, 0, 1, 1);
--ease-out: cubic-bezier(0, 0, 0.2, 1);
--ease-bounce: cubic-bezier(0.68, -0.55, 0.265, 1.55);
```

---

## 16. 可访问性

- 所有交互元素支持键盘导航
- 颜色对比度符合 WCAG 2.1 AA 标准
- 图标配合文字标签
- 表单元素有明确的标签和错误提示
- 支持屏幕阅读器
