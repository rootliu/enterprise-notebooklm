# Enterprise NotebookLM - 需求分析与设计文档

## 1. 项目概述

### 1.1 项目定位
Enterprise NotebookLM 是一个企业级商业智能(BI)个人助理平台，借鉴 Google NotebookLM 的交互模式，专为企业数据分析场景设计。系统通过自然语言交互，帮助用户连接、分析和呈现企业数据。

### 1.2 目标用户
| 角色 | 主要使用区域 | 核心需求 |
|------|-------------|---------|
| Data Engineer | 左侧栏(数据源管理) | 数据源接入、数据质量检查、Schema管理 |
| Data Scientist | 左侧栏 + 中间栏 | 数据探索、统计分析、预测建模 |
| Business Administrator | 中间栏 + 右侧栏 | 数据洞察、报告生成、决策支持 |

### 1.3 原型范围限制
- 单用户模式（不考虑多用户）
- 单项目视图（不考虑项目切换）
- 不考虑权限管理
- 不考虑深色模式
- 专注核心功能验证

---

## 2. 功能需求分析

### 2.1 左侧栏 - 数据源管理面板

#### 2.1.1 数据源类型支持（按优先级排序）

```
P0 - 核心支持:
├── CSV 文件上传
├── 数据库连接
│   ├── PostgreSQL
│   ├── MySQL
│   └── SQLite
├── PDF 文档上传
└── Web 数据源
    └── URL抓取 / API调用

P1 - 次优先级:
├── Excel (.xlsx, .xls)
├── JSON 文件
└── Data Warehouse
    ├── BigQuery
    └── Snowflake

P2 - 后续扩展:
├── Data Lake (Databricks)
├── 企业IM集成 (Slack/飞书/企业微信)
└── 图片OCR识别
```

#### 2.1.2 数据源列表功能
- **分类展示**: 按数据格式自动分类 (文件/数据库/Web)
- **排序方式**: 支持按时间、主题、自定义标签排序
- **多选操作**: 支持选择多个数据源进行联合分析
- **搜索筛选**: 快速定位目标数据源

#### 2.1.3 数据源详情视图（点击进入下一层）
当用户点击单个数据源时，左侧栏进入详情状态:

| 信息类型 | 说明 |
|---------|-----|
| 自然语言简述 | Gemini生成的数据源概要描述 |
| Schema信息 | 列名、数据类型、约束条件 |
| 数据统计 | 行数、空值率、唯一值数量 |
| 关键标签 | 自动提取的关键字段和特征 |
| 数据分布 | 数值型字段的分布情况 |
| 时间范围 | 时间序列数据的覆盖范围 |

#### 2.1.4 多数据源联合分析
选择多个数据源时:
- 自动识别可关联字段
- 生成联合数据简介
- 标识潜在的数据融合点
- 提示数据质量问题（类型不匹配、缺失值等）

---

### 2.2 中间栏 - 对话Agent面板

#### 2.2.1 自然语言交互能力
```
支持的查询类型:
├── 描述性分析
│   ├── "统计各部门的销售额"
│   ├── "计算过去12个月的平均增长率"
│   └── "找出数据中的异常值(jitter)"
├── 诊断性分析
│   ├── "为什么Q3销售额下降?"
│   └── "哪些因素影响了客户流失?"
├── 预测性分析
│   ├── "预测下季度的销售趋势"
│   └── "建立客户生命周期价值模型"
└── 规范性分析
    ├── "推荐提升转化率的策略"
    └── "优化库存管理的建议"
```

#### 2.2.2 标签交互
- 点击左侧栏标签自动生成相关查询
- 基于标签进行快速统计/分组/过滤
- 标签组合生成复杂查询

#### 2.2.3 输出内容格式
| 格式类型 | 应用场景 | 示例 |
|---------|---------|-----|
| Narrative | 文字叙述分析结果 | "销售额同比增长15%，主要驱动因素是..." |
| Tabular | 结构化数据展示 | 分组统计表、透视表 |
| Chart | 可视化图表 | 折线图、柱状图、散点图、热力图 |
| Formula | 数学公式展示 | LaTeX渲染的统计公式 |
| Citation | 数据溯源链接 | 点击跳转到原始数据位置 |

#### 2.2.4 实时数据编辑
- 在对话中插入/编辑表格数据
- 支持内联数据修正
- 修改记录追踪

---

### 2.3 右侧栏 - 工具面板

#### 2.3.1 六大核心工具详细设计

---

**工具1: Insights Report (洞察报告生成器)**

| 属性 | 说明 |
|------|-----|
| **功能** | 汇总对话内容，生成结构化洞察报告 |
| **输入** | 当前对话历史、选中的数据源、用户指定的重点标签 |
| **输出** | Markdown/HTML 格式报告 |

**报告结构:**
```
1. Executive Summary (执行摘要)
   - 核心发现 (3-5 条)
   - 关键数据指标

2. Data Overview (数据概述)
   - 使用的数据源列表
   - 数据时间范围
   - 数据质量说明

3. Analysis Details (分析详情)
   - 按对话主题分节
   - 每节包含: 问题 → 分析 → 结论
   - 嵌入相关图表和表格

4. Key Insights (关键洞察)
   - 发现的模式和趋势
   - 异常值说明
   - 相关性分析结果

5. Recommendations (建议)
   - 基于分析的行动建议
   - 进一步分析方向

6. Appendix (附录)
   - 数据源引用
   - 方法论说明
```

**UI 交互:**
```
┌─────────────────────────────────┐
│  📊 Generate Insights Report    │
├─────────────────────────────────┤
│  Report Title: [____________]   │
│                                 │
│  Include sections:              │
│  ☑ Executive Summary            │
│  ☑ Data Overview                │
│  ☑ Analysis Details             │
│  ☑ Key Insights                 │
│  ☐ Recommendations              │
│                                 │
│  Focus on tags:                 │
│  [revenue] [growth] [+ Add]     │
│                                 │
│  [Preview]  [Generate Report]   │
└─────────────────────────────────┘
```

---

**工具2: Brief Generator (一页简报生成器)**

| 属性 | 说明 |
|------|-----|
| **功能** | 生成图文并茂的一页宣传简报 |
| **输入** | 对话摘要、关键图表、用户选择的亮点 |
| **输出** | PDF/PNG 单页文档 |

**简报布局模板:**
```
┌────────────────────────────────────────┐
│  [LOGO]     报告标题          日期     │
├────────────────────────────────────────┤
│                                        │
│  ┌──────────────┐  ┌──────────────┐   │
│  │   核心指标1   │  │   核心指标2   │   │
│  │    ▲ 15%     │  │    $2.3M     │   │
│  └──────────────┘  └──────────────┘   │
│                                        │
│  ┌─────────────────────────────────┐  │
│  │                                 │  │
│  │         主要图表区域            │  │
│  │                                 │  │
│  └─────────────────────────────────┘  │
│                                        │
│  Key Findings:                         │
│  • 发现点1                             │
│  • 发现点2                             │
│  • 发现点3                             │
│                                        │
│  ┌───────────┐  ┌───────────┐         │
│  │  支撑图表1 │  │  支撑图表2 │         │
│  └───────────┘  └───────────┘         │
│                                        │
│  [Footer: 数据来源 | 生成日期]          │
└────────────────────────────────────────┘
```

**UI 交互:**
```
┌─────────────────────────────────┐
│  📄 Generate One-Page Brief     │
├─────────────────────────────────┤
│  Template: [Executive ▼]        │
│                                 │
│  Title: [________________]      │
│                                 │
│  Key Metrics (select 2-4):      │
│  ☑ Total Revenue: $2.3M         │
│  ☑ Growth Rate: 15%             │
│  ☐ Customer Count: 1,234        │
│                                 │
│  Main Chart:                    │
│  [Select from conversation ▼]   │
│                                 │
│  Key Points (max 5):            │
│  1. [Auto-filled from chat]     │
│  2. [________________]          │
│  [+ Add Point]                  │
│                                 │
│  [Preview]  [Generate Brief]    │
└─────────────────────────────────┘
```

---

**工具3: PPT Generator (演示文稿生成器)**

| 属性 | 说明 |
|------|-----|
| **功能** | 根据报告/对话生成多页演示文稿 |
| **输入** | Insights报告 或 对话历史 |
| **输出** | PPTX 文件 |

**PPT 结构:**
```
Slide 1: 标题页
  - 报告标题
  - 副标题/日期
  - 作者信息

Slide 2: 目录
  - 自动生成章节列表

Slide 3-N: 内容页
  - 每个分析主题一页
  - 标题 + 要点 + 图表布局

Slide N+1: 关键发现汇总
  - 核心洞察列表

Slide N+2: 建议与下一步
  - 行动建议

Slide 最后: 附录/Q&A
  - 数据来源
  - 联系方式
```

**UI 交互:**
```
┌─────────────────────────────────┐
│  📑 Generate Presentation       │
├─────────────────────────────────┤
│  Source: ○ Current Chat         │
│          ● Insights Report      │
│                                 │
│  Template Style:                │
│  [Corporate Blue ▼]             │
│                                 │
│  Slides to include:             │
│  ☑ Title Slide                  │
│  ☑ Table of Contents            │
│  ☑ Data Overview                │
│  ☑ Analysis (5 slides)          │
│  ☑ Key Findings                 │
│  ☐ Recommendations              │
│  ☐ Appendix                     │
│                                 │
│  Estimated: 8 slides            │
│                                 │
│  [Preview Structure]  [Generate]│
└─────────────────────────────────┘
```

---

**工具4: Data Exporter (数据导出器)**

| 属性 | 说明 |
|------|-----|
| **功能** | 导出源数据和分析结果 |
| **输入** | 选中的数据源、对话中生成的表格 |
| **输出** | CSV / Excel 文件 |

**导出选项:**
```
数据类型:
├── 原始数据源
│   ├── 完整数据
│   ├── 筛选后数据
│   └── 采样数据 (前N行)
├── 分析结果
│   ├── 统计汇总表
│   ├── 透视表结果
│   └── 预测数据
└── 对话中的表格
    └── 用户编辑的表格数据
```

**UI 交互:**
```
┌─────────────────────────────────┐
│  📁 Export Data                 │
├─────────────────────────────────┤
│  Select data to export:         │
│                                 │
│  Source Data:                   │
│  ☑ sales_2024.csv (full)        │
│  ☐ customer_db (filtered)       │
│                                 │
│  Analysis Results:              │
│  ☑ Monthly Summary Table        │
│  ☑ Regional Breakdown           │
│  ☐ Prediction Results           │
│                                 │
│  Chat Tables:                   │
│  ☑ Edited Table #1              │
│                                 │
│  Format: ○ CSV  ● Excel         │
│                                 │
│  Options:                       │
│  ☑ Include headers              │
│  ☑ Include metadata sheet       │
│  ☐ Separate files per source    │
│                                 │
│  [Export Selected]              │
└─────────────────────────────────┘
```

---

**工具5: Podcast Generator (播客生成器)**

| 属性 | 说明 |
|------|-----|
| **功能** | 将分析报告转换为播客音频 |
| **输入** | Insights报告 或 对话摘要 |
| **输出** | MP3/WAV 音频文件 |
| **状态** | 设计阶段 - 暂不实现 |

**设计预留:**
```
播客结构:
├── 开场白 (自动生成)
├── 数据背景介绍
├── 核心发现讲解
│   ├── 发现1 + 解释
│   ├── 发现2 + 解释
│   └── ...
├── 总结与建议
└── 结束语

配置选项 (未来):
├── 语音风格 (专业/轻松/新闻)
├── 语速调节
├── 背景音乐
└── 时长控制
```

---

**工具6: Data Source Creator (数据源创建器)**

| 属性 | 说明 |
|------|-----|
| **功能** | 将分析结果保存为新的数据源 |
| **输入** | 多选的原始数据源 + 对话中的表格 + 强调的标签 |
| **输出** | 新的内部数据源，添加到左侧栏 |

**创建流程:**
```
1. 选择数据来源
   ├── 原始数据源 (可多选)
   ├── 对话中生成/编辑的表格
   └── 筛选条件

2. 配置合并逻辑
   ├── JOIN 类型 (Inner/Left/Right/Full)
   ├── 关联字段映射
   └── 冲突处理规则

3. 选择输出字段
   ├── 保留字段选择
   ├── 重命名
   └── 计算字段

4. 生成元数据
   ├── 数据源名称
   ├── 自动简介 (Gemini生成)
   └── 标签提取
```

**UI 交互:**
```
┌─────────────────────────────────┐
│  ➕ Create New Data Source      │
├─────────────────────────────────┤
│  Name: [new_combined_data]      │
│                                 │
│  Include from:                  │
│  ☑ sales_2024.csv               │
│  ☑ Monthly Summary (from chat)  │
│  ☐ customer_db                  │
│                                 │
│  Join Settings:                 │
│  Type: [Left Join ▼]            │
│  On: sales.customer_id =        │
│      summary.cust_id            │
│                                 │
│  Output Columns:                │
│  ☑ date                         │
│  ☑ revenue                      │
│  ☑ customer_name                │
│  ☐ internal_id                  │
│  [+ Add Calculated Field]       │
│                                 │
│  Tags to emphasize:             │
│  [revenue] [monthly] [+ Add]    │
│                                 │
│  [Preview Data]  [Create]       │
└─────────────────────────────────┘
```

---

#### 2.3.2 生成内容管理
- 所有生成的内容显示在工具下方
- 支持列表预览（缩略图/文件名/日期）
- 双击弹出详情窗口
- 右键菜单：下载、重命名、删除、在Agent中打开
- 可在中间栏Agent中通过自然语言管理

---

### 2.4 右侧栏 - 数据详情面板

当用户点击中间栏的数据溯源链接时:
- 显示原始数据的详细信息
- 高亮相关数据行/列
- 支持数据探索和钻取
- 可以从详情面板直接发起新的查询

---

## 3. 非功能需求

### 3.1 性能要求
- 数据源加载: < 3秒 (100MB以内)
- Agent响应: 首字符 < 2秒
- 图表渲染: < 1秒

### 3.2 可用性要求
- 响应式布局 (最小宽度 1280px)
- 键盘快捷键支持
- 操作可撤销

### 3.3 兼容性要求
- 浏览器: Chrome 90+, Firefox 90+, Safari 14+, Edge 90+
- 文件格式: UTF-8编码支持

---

## 4. 系统架构设计

### 4.1 整体架构
```
┌─────────────────────────────────────────────────────────────────┐
│                        Frontend (React)                         │
├─────────────┬─────────────────────────┬─────────────────────────┤
│  DataPanel  │      ChatPanel          │      ToolPanel          │
│  (左侧栏)    │      (中间栏)            │      (右侧栏)            │
└──────┬──────┴───────────┬─────────────┴────────────┬────────────┘
       │                  │                          │
       ▼                  ▼                          ▼
┌─────────────────────────────────────────────────────────────────┐
│                      API Gateway (Express)                       │
├─────────────┬─────────────────────────┬─────────────────────────┤
│ DataSource  │      Agent API          │      Export API         │
│ Service     │                         │                         │
└──────┬──────┴───────────┬─────────────┴────────────┬────────────┘
       │                  │                          │
       ▼                  ▼                          ▼
┌─────────────┐  ┌─────────────────┐  ┌─────────────────────────┐
│ Data        │  │ LLM Service     │  │ Export Service          │
│ Connectors  │  │ (Gemini)        │  │ (PDF/PPT/CSV)           │
└─────────────┘  └─────────────────┘  └─────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────┐
│              External Data Sources                               │
├─────────────┬─────────────┬─────────────┬───────────────────────┤
│ Database    │ Files       │ Web/API     │ (Future: Warehouse)   │
└─────────────┴─────────────┴─────────────┴───────────────────────┘
```

### 4.2 前端组件架构
```
src/
├── components/
│   ├── Layout/
│   │   └── ThreeColumnLayout.tsx    # 三栏布局容器
│   ├── DataPanel/                   # 左侧栏
│   │   ├── DataSourceList.tsx       # 数据源列表
│   │   ├── DataSourceCard.tsx       # 数据源卡片
│   │   ├── DataSourceDetail.tsx     # 数据源详情
│   │   ├── DataSourceUpload.tsx     # 上传组件
│   │   ├── DataSourceConnect.tsx    # 数据库连接配置
│   │   └── TagList.tsx              # 标签列表
│   ├── ChatPanel/                   # 中间栏
│   │   ├── ChatContainer.tsx        # 对话容器
│   │   ├── MessageList.tsx          # 消息列表
│   │   ├── MessageItem.tsx          # 单条消息
│   │   ├── InputArea.tsx            # 输入区域
│   │   ├── ChartRenderer.tsx        # 图表渲染
│   │   ├── TableEditor.tsx          # 表格编辑器
│   │   └── FormulaRenderer.tsx      # 公式渲染
│   ├── ToolPanel/                   # 右侧栏
│   │   ├── ToolList.tsx             # 工具列表
│   │   ├── InsightReport.tsx        # 报告工具
│   │   ├── BriefGenerator.tsx       # 简报工具
│   │   ├── PPTGenerator.tsx         # PPT工具
│   │   ├── DataExporter.tsx         # 导出工具
│   │   ├── PodcastGenerator.tsx     # 播客工具 (预留)
│   │   ├── DataSourceCreator.tsx    # 数据源创建
│   │   ├── GeneratedContent.tsx     # 生成内容列表
│   │   └── DataDetailView.tsx       # 数据详情视图
│   └── Common/
│       ├── Modal.tsx
│       ├── Dropdown.tsx
│       ├── Button.tsx
│       ├── Input.tsx
│       ├── Checkbox.tsx
│       └── Loading.tsx
├── hooks/
│   ├── useDataSources.ts
│   ├── useChat.ts
│   └── useExport.ts
├── services/
│   ├── api.ts
│   ├── dataSource.ts
│   ├── chat.ts
│   └── export.ts
├── store/
│   ├── dataSourceStore.ts
│   ├── chatStore.ts
│   └── uiStore.ts
└── types/
    ├── dataSource.ts
    ├── chat.ts
    └── export.ts
```

### 4.3 后端服务架构
```
server/src/
├── routes/
│   ├── dataSource.ts      # 数据源相关API
│   ├── chat.ts            # 对话Agent API
│   ├── export.ts          # 导出相关API
│   └── search.ts          # Web搜索API
├── services/
│   ├── dataSource/
│   │   ├── connector.ts   # 数据源连接器工厂
│   │   ├── database.ts    # 数据库连接 (PG/MySQL/SQLite)
│   │   ├── file.ts        # 文件解析服务 (CSV/PDF)
│   │   └── web.ts         # Web数据抓取
│   ├── llm/
│   │   ├── gemini.ts      # Gemini API 集成
│   │   ├── agent.ts       # Agent核心逻辑
│   │   ├── tools.ts       # Agent工具定义
│   │   └── prompt.ts      # Prompt模板
│   ├── analysis/
│   │   ├── statistics.ts  # 统计分析
│   │   ├── prediction.ts  # 预测模型
│   │   └── anomaly.ts     # 异常检测
│   └── export/
│       ├── report.ts      # Markdown报告生成
│       ├── brief.ts       # 一页简报生成
│       ├── ppt.ts         # PPT生成
│       └── csv.ts         # CSV导出
├── models/
│   ├── DataSource.ts
│   ├── Conversation.ts
│   └── GeneratedContent.ts
└── utils/
    ├── parser.ts
    └── validator.ts
```

---

## 5. 数据模型设计

### 5.1 核心实体

```typescript
// 数据源
interface DataSource {
  id: string;
  name: string;
  type: 'database' | 'file' | 'web' | 'generated';
  format: 'csv' | 'pdf' | 'postgresql' | 'mysql' | 'sqlite' | 'api';
  connectionConfig?: DatabaseConfig | WebConfig;
  fileInfo?: FileInfo;
  schema: SchemaInfo;
  summary: string;           // Gemini生成的自然语言简述
  tags: Tag[];               // 自动提取的标签
  statistics: DataStatistics;
  createdAt: Date;
  updatedAt: Date;
}

// 数据库连接配置
interface DatabaseConfig {
  host: string;
  port: number;
  database: string;
  username: string;
  password: string;          // 加密存储
  ssl?: boolean;
}

// Web数据源配置
interface WebConfig {
  url: string;
  method: 'GET' | 'POST';
  headers?: Record<string, string>;
  refreshInterval?: number;  // 自动刷新间隔(分钟)
}

// Schema信息
interface SchemaInfo {
  columns: Column[];
  primaryKey?: string[];
  rowCount: number;
  sampleData: Record<string, any>[];
}

interface Column {
  name: string;
  type: 'string' | 'number' | 'date' | 'boolean' | 'object';
  nullable: boolean;
  description?: string;
  statistics?: ColumnStatistics;
}

interface ColumnStatistics {
  min?: number;
  max?: number;
  mean?: number;
  median?: number;
  nullCount: number;
  uniqueCount: number;
  distribution?: { value: string; count: number }[];
}

// 标签
interface Tag {
  id: string;
  name: string;
  type: 'column' | 'metric' | 'dimension' | 'custom';
  sourceColumn?: string;
  description?: string;
}

// 对话消息
interface Message {
  id: string;
  role: 'user' | 'assistant' | 'system';
  content: string;
  contentType: 'text' | 'table' | 'chart' | 'formula' | 'mixed';
  attachments?: Attachment[];
  citations?: Citation[];
  timestamp: Date;
}

interface Attachment {
  type: 'table' | 'chart' | 'image';
  data: any;
  editable?: boolean;
}

// 数据引用
interface Citation {
  dataSourceId: string;
  dataSourceName: string;
  location: {
    rowStart?: number;
    rowEnd?: number;
    columns?: string[];
  };
  preview: string;
}

// 生成内容
interface GeneratedContent {
  id: string;
  type: 'report' | 'brief' | 'ppt' | 'csv' | 'podcast' | 'datasource';
  name: string;
  sourceConversationId: string;
  sourceDataSourceIds: string[];
  filePath?: string;         // 文件存储路径
  previewUrl?: string;       // 预览URL
  metadata: Record<string, any>;
  createdAt: Date;
}
```

### 5.2 状态管理设计 (Zustand)

```typescript
// 数据源状态
interface DataSourceState {
  dataSources: DataSource[];
  selectedIds: string[];
  currentDetail: DataSource | null;
  viewMode: 'list' | 'detail';
  sortBy: 'time' | 'type' | 'name';
  filterType: string | null;
  searchQuery: string;

  // Actions
  addDataSource: (source: DataSource) => void;
  removeDataSource: (id: string) => void;
  updateDataSource: (id: string, updates: Partial<DataSource>) => void;
  selectDataSource: (id: string) => void;
  toggleSelection: (id: string) => void;
  clearSelection: () => void;
  setViewMode: (mode: 'list' | 'detail') => void;
  setCurrentDetail: (source: DataSource | null) => void;
}

// 对话状态
interface ChatState {
  messages: Message[];
  isLoading: boolean;
  streamingContent: string;
  activeDataSourceIds: string[];
  editingTableId: string | null;

  // Actions
  sendMessage: (content: string) => Promise<void>;
  addMessage: (message: Message) => void;
  updateMessage: (id: string, updates: Partial<Message>) => void;
  setActiveDataSources: (ids: string[]) => void;
  clearChat: () => void;
}

// UI状态
interface UIState {
  leftPanelWidth: number;
  rightPanelWidth: number;
  rightPanelMode: 'tools' | 'detail';
  activeModal: string | null;
  activeTool: string | null;

  // Actions
  setLeftPanelWidth: (width: number) => void;
  setRightPanelWidth: (width: number) => void;
  setRightPanelMode: (mode: 'tools' | 'detail') => void;
  openModal: (modalId: string) => void;
  closeModal: () => void;
  setActiveTool: (toolId: string | null) => void;
}

// 生成内容状态
interface GeneratedContentState {
  contents: GeneratedContent[];

  // Actions
  addContent: (content: GeneratedContent) => void;
  removeContent: (id: string) => void;
  updateContent: (id: string, updates: Partial<GeneratedContent>) => void;
}
```

---

## 6. 技术选型

### 6.1 前端技术栈
| 类别 | 技术 | 说明 |
|------|-----|------|
| 框架 | React 18 + TypeScript | 组件化开发 |
| 构建 | Vite | 快速开发体验 |
| 状态管理 | Zustand | 轻量级状态管理 |
| UI组件 | Radix UI + Tailwind CSS | 无样式组件 + 原子化CSS |
| 图表 | ECharts | 数据可视化 |
| 表格 | TanStack Table | 高性能表格 |
| 公式 | KaTeX | LaTeX数学公式渲染 |
| Markdown | React-Markdown | Markdown渲染 |
| 拖拽 | react-resizable-panels | 面板大小调整 |

### 6.2 后端技术栈
| 类别 | 技术 | 说明 |
|------|-----|------|
| 运行时 | Node.js 20+ | LTS版本 |
| 框架 | Express 5 | Web框架 |
| 语言 | TypeScript | 类型安全 |
| ORM | Prisma | 数据库访问 |
| 数据库 | SQLite (原型) | 数据持久化 |
| 文件处理 | Multer + pdf-parse + csv-parser | 文件上传解析 |
| 数据库连接 | pg / mysql2 / better-sqlite3 | 多数据库支持 |

### 6.3 AI 技术栈
| 类别 | 技术 | 说明 |
|------|-----|------|
| LLM | Google Gemini | 自然语言理解与生成 |
| SDK | @google/generative-ai | Gemini官方SDK |
| Agent | 自研 | 基于Function Calling |

### 6.4 导出工具链
| 输出格式 | 技术方案 |
|---------|---------|
| Markdown | 模板渲染 |
| PDF | Puppeteer + HTML模板 |
| PPTX | PptxGenJS |
| CSV | csv-stringify |

---

## 7. 参考资料

- [Google NotebookLM](https://notebooklm.google.com/)
- [Google Gemini API](https://ai.google.dev/docs)
- [Radix UI](https://www.radix-ui.com/)
- [Tailwind CSS](https://tailwindcss.com/)
