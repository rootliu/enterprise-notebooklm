# Enterprise NotebookLM 设计文档

## 1. 功能概述

本应用是一个基于 Gemini Pro 3 的智能文档分析与问答系统，支持文件上传、AI 分析、交互式问答和会话管理。

## 2. 核心功能模块

### 2.1 文件上传与分析

#### 支持的文件格式
- CSV (.csv)
- Excel (.xlsx, .xls)
- PDF (.pdf)
- HTML (.html, .htm)

#### 上传流程
1. 用户点击左边栏"离线"区域的上传按钮
2. 选择本地文件
3. 应用将文件上传到后端
4. 后端调用 Gemini Pro 3 进行分析：
   - 生成文件摘要 (Summary)
   - 提取关键词标签 (Tags)，不超过 10 个，基于管理学常用术语
5. 分析完成后，文件显示在左边栏列表中

#### 文件显示
- 默认显示：文件名、文件大小、上传时间
- 点击展开：显示 Summary 和 Tags

### 2.2 标签筛选系统

#### 筛选功能
- 点击单个 Tag：筛选包含该 Tag 的所有文件
- 多选 Tags：筛选包含任意选中 Tag 的文件（OR 逻辑）
- 手动输入 Tag：支持直接输入 Tag 进行筛选
- 搜索框：根据文件名和 Summary 进行模糊搜索

#### UI 设计
- Tag 筛选区域位于文件列表上方
- 已选 Tags 以 chip 形式显示，可点击 x 移除
- 搜索框支持实时筛选

### 2.3 文件选择与 Context 管理

#### 选择模式
- 单选：选择一个文件
- 多选：选择多个文件（Ctrl/Cmd + 点击）

#### Context 类型
1. **Summary Context**：仅使用文件的 Summary 作为 context
2. **Full Context**：使用完整文件内容作为 context（通过中间栏上传）

#### 导入数据源
- 从左边栏选择文件后，点击中间栏"导入数据源"按钮
- 选中的文件被引入为当前会话的 context

### 2.4 中间栏交互式问答

#### 问答流程
1. 用户在输入框输入问题
2. 应用将问题 + context（选中文件的 Summary 或完整内容）发送给 Gemini Pro 3
3. 获取 AI 回复并显示

#### 上传文件（中间栏）
- 点击中间栏上传按钮
- 上传的文件作为 **完整内容** 加入 context
- 同时生成 Summary 和 Tags，文件也显示在左边栏

### 2.5 对话导出

#### 导出格式
- DOCX
- Markdown

#### 导出流程
1. 用户选择对话内容
2. 选择导出格式
3. 生成文件并下载
4. 导出的文件自动添加到左边栏
5. 自动生成 Summary 和 Tags

### 2.6 会话管理

#### 保存会话
- 点击"保存会话"按钮
- 当前会话保存为 Markdown 文件
- 文件添加到左边栏

#### 打开历史会话
- 选择历史 Markdown 文件
- 作为 context 引入
- **替换** 当前 context（非追加）

#### 新建 Context
- 点击"新 Context"按钮
- 清空当前所有 context
- 开始全新会话

## 3. 数据模型

### 3.1 OfflineFile（文件）
```typescript
interface OfflineFile {
  id: string;
  name: string;
  format: 'csv' | 'excel' | 'pdf' | 'html' | 'markdown' | 'docx';
  size: string;
  uploadedAt: Date;
  summary: string;
  tags: string[];
  filePath: string;        // 后端存储路径
  contentType: 'summary' | 'full';  // context 使用方式
  status: 'uploading' | 'analyzing' | 'ready' | 'error';
  errorMessage?: string;
}
```

### 3.2 ChatMessage（对话消息）
```typescript
interface ChatMessage {
  id: string;
  role: 'user' | 'assistant';
  content: string;
  timestamp: Date;
  contextFiles?: string[];  // 关联的文件 ID
}
```

### 3.3 Session（会话）
```typescript
interface Session {
  id: string;
  name: string;
  createdAt: Date;
  updatedAt: Date;
  messages: ChatMessage[];
  contextFileIds: string[];
}
```

### 3.4 Context（上下文）
```typescript
interface ContextItem {
  fileId: string;
  type: 'summary' | 'full';
  content: string;
}

interface CurrentContext {
  items: ContextItem[];
  sessionId?: string;
}
```

## 4. API 设计

### 4.1 文件相关

#### POST /api/files/upload
上传并分析文件
```
Request: multipart/form-data
  - file: File
  - contextType: 'summary' | 'full'

Response: {
  id: string,
  name: string,
  format: string,
  size: string,
  summary: string,
  tags: string[],
  status: string
}
```

#### GET /api/files
获取文件列表
```
Query:
  - tags?: string[]  // 筛选标签
  - search?: string  // 搜索关键词

Response: OfflineFile[]
```

#### GET /api/files/:id
获取单个文件详情
```
Response: OfflineFile
```

#### GET /api/files/:id/content
获取文件完整内容
```
Response: {
  content: string
}
```

#### DELETE /api/files/:id
删除文件

### 4.2 对话相关

#### POST /api/chat
发送消息
```
Request: {
  message: string,
  contextFileIds: string[],
  contextType: 'summary' | 'full'
}

Response: {
  id: string,
  role: 'assistant',
  content: string,
  timestamp: Date
}
```

#### POST /api/chat/export
导出对话
```
Request: {
  messageIds: string[],
  format: 'docx' | 'markdown'
}

Response: {
  fileId: string,  // 新生成的文件 ID
  downloadUrl: string
}
```

### 4.3 会话相关

#### POST /api/sessions
保存当前会话
```
Request: {
  name: string,
  messages: ChatMessage[],
  contextFileIds: string[]
}

Response: {
  sessionId: string,
  fileId: string  // 保存的 markdown 文件 ID
}
```

#### GET /api/sessions
获取历史会话列表
```
Response: Session[]
```

#### GET /api/sessions/:id
加载历史会话
```
Response: Session
```

## 5. UI 组件设计

### 5.1 左边栏 (DataPanel)

```
┌─────────────────────────────┐
│  [搜索框]                    │
├─────────────────────────────┤
│  Tags: [财务] [销售] [+]     │
├─────────────────────────────┤
│  ┌─────────────────────────┐│
│  │ 📄 文件上传区域          ││
│  │    点击或拖拽上传        ││
│  └─────────────────────────┘│
├─────────────────────────────┤
│  文件列表                    │
│  ┌─────────────────────────┐│
│  │ ☐ sales_q3.xlsx         ││
│  │   2.3 MB · 5分钟前       ││
│  │   ▼ 展开                 ││
│  │   摘要: Q3销售数据...    ││
│  │   [财务][销售][Q3]       ││
│  └─────────────────────────┘│
│  ┌─────────────────────────┐│
│  │ ☐ report.pdf            ││
│  │   ...                    ││
│  └─────────────────────────┘│
└─────────────────────────────┘
```

### 5.2 中间栏 (ChatPanel)

```
┌─────────────────────────────────────┐
│  会话: 未保存 ○  [保存] [新Context] │
├─────────────────────────────────────┤
│  Context: [sales_q3.xlsx ×]         │
│           [report.pdf ×]            │
│           [+ 导入数据源]            │
├─────────────────────────────────────┤
│                                     │
│  对话内容区域                        │
│                                     │
│  User: 分析Q3销售趋势               │
│                                     │
│  AI: 根据数据分析...                │
│      ☐ 选择导出                     │
│                                     │
├─────────────────────────────────────┤
│  [上传] [链接]  |  [预测][拟合]...  │
│  ┌─────────────────────────────────┐│
│  │ 输入问题...              [发送] ││
│  └─────────────────────────────────┘│
│                           [Gemini ▼]│
└─────────────────────────────────────┘
```

## 6. 状态管理 (Zustand Store)

### 6.1 fileStore
```typescript
interface FileState {
  files: OfflineFile[];
  selectedFileIds: string[];
  filterTags: string[];
  searchQuery: string;

  // Actions
  addFile: (file: OfflineFile) => void;
  updateFile: (id: string, updates: Partial<OfflineFile>) => void;
  deleteFile: (id: string) => void;
  selectFile: (id: string) => void;
  deselectFile: (id: string) => void;
  toggleFileSelection: (id: string) => void;
  clearSelection: () => void;
  setFilterTags: (tags: string[]) => void;
  addFilterTag: (tag: string) => void;
  removeFilterTag: (tag: string) => void;
  setSearchQuery: (query: string) => void;

  // Computed
  getFilteredFiles: () => OfflineFile[];
  getSelectedFiles: () => OfflineFile[];
}
```

### 6.2 contextStore
```typescript
interface ContextState {
  contextItems: ContextItem[];

  // Actions
  addToContext: (fileId: string, type: 'summary' | 'full') => void;
  removeFromContext: (fileId: string) => void;
  clearContext: () => void;
  replaceContext: (items: ContextItem[]) => void;
}
```

### 6.3 sessionStore
```typescript
interface SessionState {
  currentSessionId: string | null;
  isModified: boolean;
  sessions: Session[];

  // Actions
  saveSession: (name: string) => Promise<string>;
  loadSession: (id: string) => Promise<void>;
  newSession: () => void;
}
```

## 7. 测试计划

### 7.1 单元测试

#### 文件处理
- [ ] 测试 CSV 文件解析
- [ ] 测试 Excel 文件解析
- [ ] 测试 PDF 文件解析
- [ ] 测试 HTML 文件解析
- [ ] 测试文件大小限制验证
- [ ] 测试不支持的文件格式拒绝

#### AI 分析
- [ ] 测试 Summary 生成（字数限制、内容相关性）
- [ ] 测试 Tags 提取（数量限制、管理学术语）
- [ ] 测试空文件处理
- [ ] 测试大文件处理

#### 标签筛选
- [ ] 测试单标签筛选
- [ ] 测试多标签筛选（OR 逻辑）
- [ ] 测试手动输入标签
- [ ] 测试搜索功能（文件名匹配）
- [ ] 测试搜索功能（Summary 匹配）

#### Context 管理
- [ ] 测试添加文件到 Context
- [ ] 测试从 Context 移除文件
- [ ] 测试清空 Context
- [ ] 测试 Context 替换

### 7.2 集成测试

#### 上传流程
- [ ] 测试完整上传流程（上传 → 分析 → 显示）
- [ ] 测试上传进度显示
- [ ] 测试上传失败处理
- [ ] 测试并发上传

#### 问答流程
- [ ] 测试 Summary Context 问答
- [ ] 测试 Full Context 问答
- [ ] 测试多文件 Context 问答
- [ ] 测试空 Context 问答

#### 导出功能
- [ ] 测试 DOCX 导出
- [ ] 测试 Markdown 导出
- [ ] 测试导出文件自动添加到左边栏

#### 会话管理
- [ ] 测试保存会话
- [ ] 测试加载历史会话
- [ ] 测试新建会话清空 Context

### 7.3 E2E 测试

#### 用户场景
- [ ] 场景1: 上传文件 → 查看分析结果 → 问答
- [ ] 场景2: 多文件选择 → 导入 Context → 对比分析
- [ ] 场景3: 筛选标签 → 选择文件 → 问答 → 导出
- [ ] 场景4: 保存会话 → 新建会话 → 加载历史会话

### 7.4 性能测试

- [ ] 测试大文件上传（10MB+）
- [ ] 测试批量文件上传（10+文件）
- [ ] 测试长对话性能
- [ ] 测试大量文件列表渲染

## 8. 管理学常用标签参考

### 财务管理
财务、预算、成本、收入、利润、现金流、资产、负债、投资、ROI

### 运营管理
运营、流程、效率、质量、库存、供应链、生产、物流、KPI、绩效

### 市场营销
市场、营销、销售、客户、品牌、推广、渠道、定价、竞争、增长

### 人力资源
人力、招聘、培训、绩效、薪酬、组织、团队、领导力、文化、员工

### 战略管理
战略、规划、目标、风险、机会、竞争、创新、转型、合作、并购

### 数据分析
数据、分析、报表、趋势、预测、对比、统计、指标、仪表盘、可视化

## 9. 技术栈

### 前端
- React 18
- TypeScript
- Zustand (状态管理)
- Tailwind CSS
- Radix UI (组件库)
- Vite (构建工具)

### 后端
- Node.js / Express 或 Python / FastAPI
- Google Gemini 3 Pro API
- 文件解析库:
  - csv-parser (CSV)
  - xlsx (Excel)
  - pdf-parse (PDF)
  - cheerio (HTML)
- docx (DOCX 生成)

### AI 模型配置（重要 - 请勿修改）

| 配置项 | 值 | 说明 |
|--------|-----|------|
| **API Key** | `AIzaSyCt7PZfa7OH2PkNoMGQQ6PQGwVybqlK3EE` | 硬编码，不从环境变量读取 |
| **模型** | `gemini-3-pro-preview` | Gemini 3 Pro 模型 |
| **SDK** | `@google/generative-ai` | Google Generative AI SDK |

> **重要**: 此配置为项目固定配置，所有 AI 分析功能（文件分析、对话、摘要生成）必须使用此配置。实现时必须硬编码，不得从环境变量或配置文件读取。

**代码示例**:
```typescript
// server/src/services/geminiService.ts
import { GoogleGenerativeAI } from '@google/generative-ai';

// Hardcoded Gemini API Key - DO NOT CHANGE
const GEMINI_API_KEY = 'AIzaSyCt7PZfa7OH2PkNoMGQQ6PQGwVybqlK3EE';

// Model configuration - DO NOT CHANGE
const GEMINI_MODEL = 'gemini-3-pro-preview';

const genAI = new GoogleGenerativeAI(GEMINI_API_KEY);
const model = genAI.getGenerativeModel({ model: GEMINI_MODEL });
```

### 测试
- Vitest (单元测试)
- Playwright (E2E 测试)
- pytest (后端测试)

## 10. 开发阶段

### Phase 1: 基础文件上传与分析
1. 后端文件上传 API
2. Gemini Pro 3 集成（Summary + Tags）
3. 前端上传组件
4. 文件列表显示

### Phase 2: 标签筛选与搜索
1. 标签筛选 UI
2. 搜索功能
3. 多选标签

### Phase 3: Context 管理与问答
1. Context Store
2. 导入数据源功能
3. 问答 API
4. 对话 UI

### Phase 4: 导出与会话管理
1. 导出 DOCX/Markdown
2. 保存会话
3. 加载历史会话
4. 新建 Context

### Phase 5: 优化与测试
1. 性能优化
2. 错误处理
3. 完整测试覆盖
